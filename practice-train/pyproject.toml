[tool.poetry]
name = "practice-deploy"
version = "0.1.0"
description = ""
authors = ["Thomas Belin <thomasbelin@local>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
